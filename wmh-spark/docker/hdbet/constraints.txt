# nnunetv2 (hd-bet 2.0 dep) requires torch>=2.1.2, so 2.0.0 is too old. Pin a CPU
# pair from https://download.pytorch.org/whl/cpu to stop pip from choosing CUDA torch.
torch==2.4.0
torchvision==0.19.0
